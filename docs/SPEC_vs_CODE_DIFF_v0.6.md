# Сравнение документации v0.6 с реализацией v0.4.0

## Категория A: КРИТИЧНЫЕ различия (требуют архитектурных изменений)

### A1. Терминология: Органы vs Контейнеры

**Документация:**
- Четкое разделение: **Органы** (Liver, Pancreas, Muscles, Kidneys) и **Контейнеры** (LiverContainer, BGContainer, Effect Containers)
- Органы имеют Rules и логику
- Контейнеры хранят вещества (глюкозу, эффекты)

**Код:**
- `src/core/types.ts`:
  - `ContainerId = 'liver' | 'bg' | 'metforminEffect' | 'exerciseEffect'`
  - `OrganId = 'liver' | 'pancreas' | 'muscles'`
- Смешение: 'liver' используется и как орган и как контейнер
- Нет четкого разделения между LiverContainer (хранилище) и Liver (орган с правилами)

**Влияние:** Путаница в архитектуре, сложность понимания кода

---

### A2. LoadType: Treatment vs Substance

**Документация:**
- `LoadType` имеет значения: `Glucose`, `Substance`, `Insulin` (отложен)
- `Substance` — абстрактное вещество для интервенций

**Код:**
- `src/core/types.ts:9`: `LoadType = 'Glucose' | 'Treatment'`
- Используется `Treatment` вместо `Substance`

**Влияние:** Несоответствие терминологии

---

### A3. Pancreas как орган - отсутствует в симуляции

**Документация (5.6.3):**
- Pancreas — полноценный орган с Rules
- Pancreas **регулирует работу мышц**, назначая им Tier скорости утилизации
- Rules:
  - BG ≥ 150 → Activate Muscle Tier 3
  - BG ≤ 100 → Activate Muscle Tier 0
  - Manual action → Tier 4

**Код:**
- `src/core/simulation/SimulationEngine.ts`: Нет отдельного класса/модуля Pancreas
- Логика Pancreas встроена напрямую в Muscle Rules (`src/config/organRules.json`)
- `pancreasBoost` существует как boost state, но не как орган

**Влияние:** Архитектурное несоответствие — логика распределена неправильно

---

### A4. Effect Containers: пороги vs простые контейнеры

**Документация (5.7.2):**
- Effect Containers имеют **Thresholds** (пороги)
- Каждый порог активирует специфичный Effect
- Metformin: 5 порогов по 20 единиц → каждый снижает Degradation Tier на 1
- Exercise: 2 порога (10 и 60) → увеличивают Muscle Tier на +1 и +2

**Код:**
- `src/core/simulation/SimulationEngine.ts`: простые контейнеры
  ```typescript
  containers: {
    metforminEffect: number,
    exerciseEffect: number
  }
  ```
- Нет порогов, нет Effect активации
- Exercise используется как модификатор в Rule Engine

**Влияние:** Упрощенная модель, не соответствует спецификации

---

### A5. Degradation System: отсутствие Tier-based системы

**Документация (5.8):**
- **Degradation Buffer** → накопление единиц деградации
- **Degradation Tiers** → пороги буфера активируют степени
- Каждый орган имеет свой Degradation Tier (#1-5)
- Tier эффекты:
  - Liver: уменьшение LiverContainerCapacity (100→80→70→60→50→40)
  - Pancreas: снижение максимального GlucoseTransferRateTier (-1, -2, -3, -4)

**Код:**
- `src/core/types.ts:101-104`:
  ```typescript
  interface SimpleDegradation {
    liver: number;
    pancreas: number;
  }
  ```
- Простые числовые значения без Tiers
- Нет Degradation Buffer
- Нет порогов и эффектов

**Влияние:** Критичное упрощение системы прогрессии

---

### A6. Muscle Rate Tiers: несоответствие значений

**Документация (5.6.4):**
```
GlucoseTransferRateTiers:
  Tier 0: 0
  Tier 1: 20
  Tier 2: 30
  Tier 3: 50
  Tier 4: 70
  Tier 5: 90
```

**Код:**
- `src/core/simulation/SimulationEngine.ts:113`:
  ```typescript
  muscleDrainRates: [0, 30, 35, 40, 45, 50]
  ```
- Совершенно другая прогрессия (линейная 30→50 vs экспоненциальная 20→90)

**Влияние:** Критичное несоответствие баланса

---

## Категория B: ВАЖНЫЕ различия (требуют рефакторинга)

### B1. Ship Parameters: различия в именовании

**Документация (5.4.2):**
- `Id` — уникальное имя
- `DisplayedName` — отображаемое имя
- `Icon` — иконка
- `Size` — типоразмер
- `Load` — объем груза
- `LoadType` — тип груза
- `TargetContainer` — целевой контейнер

**Код:**
- `src/core/types.ts:61-70`:
  ```typescript
  interface Ship {
    id: string;
    name: string;        // вместо DisplayedName
    emoji: string;       // вместо Icon
    size: ShipSize;
    load: number;
    carbs?: number;      // дополнительное поле, нет в документации
    loadType: LoadType;
    targetContainer: ContainerId;
    description?: string; // дополнительное поле
  }
  ```

**Влияние:** Несоответствие интерфейса

---

### B2. Liver Transfer Rules: структура

**Документация (5.6.1):**
```
GlucoseTransferRateTiers: [0, 50, 75]
Rules:
  1. BG ≤ 100 → Tier 1
  2. BG ≥ 200 → Tier 0
  3. LiverContainer = 100% → Tier 2
  4. Manual (PlayerTap) → Tier 2, Cooldown 3h
```

**Код:**
- `src/config/organRules.json`:
  ```json
  "liver": {
    "rates": [0, 50, 75],
    "rules": [
      {"id": "liver_boost_active", ...},
      {"id": "liver_full", ...},
      {"id": "bg_below_target", ...},
      {"id": "bg_above_high", ...}
    ]
  }
  ```
- Есть правила, но структура и приоритеты могут отличаться

**Влияние:** Нужна проверка корректности правил

---

### B3. Pancreas Boost UI: неправильное название

**Документация (3.4):**
- "временное усиление работы поджелудочной железы"

**Код:**
- `src/components/simulation/SimulationPhase.tsx:221`:
  ```tsx
  <BoostButton
    label="Muscle Boost"  // Неправильно! Должно быть "Pancreas Boost"
    emoji="💪"
    boost={simState.pancreasBoost}
  ```

**Влияние:** Вводит в заблуждение пользователя

---

### B4. Effect Containers: DecayRate значения

**Документация (5.8.4, 5.8.5):**
- Metformin: DecayDurationTarget = ~18 часов полного эффекта
- Exercise: DecayDurationTarget = ~1 час полного эффекта

**Код:**
- `src/core/simulation/SimulationEngine.ts:114-115`:
  ```typescript
  metforminDecayRate: 7,    // TODO: Excel v0.6 specifies 10/hour
  exerciseDecayRate: 100,   // 1 hour to full decay
  ```
- Metformin: 7/hour (неправильно, должно быть ~5.5 для 18 часов)
- Exercise: 100/hour (правильно для 1 часа)

**Влияние:** Несоответствие баланса для Metformin

---

### B5. Kidneys: отсутствие реализации

**Документация (5.6.5):**
- Подробное описание работы Kidneys
- Rules, KidneyContainer, GlucoseTransferRateTiers
- Excretion механика

**Код:**
- `src/components/simulation/GlucoseParticleSystem.tsx:191`:
  ```tsx
  bgToKidneysRate={interpolated.bg > 180 ? Math.min((interpolated.bg - 180) * 0.1, 20) : 0}
  ```
- Только визуальные частицы, нет логики симуляции

**Влияние:** Неполная реализация

---

## Категория C: НЕСУЩЕСТВЕННЫЕ различия (косметические)

### C1. Parameter naming conventions

**Документация:**
- `CurrentAmount` — текущее количество в Effect Container
- `MaxCapacity` — максимальная емкость

**Код:**
- `level` — текущий уровень
- `capacity` — емкость

**Влияние:** Минимальное, стиль кода

---

### C2. UI elements

**Документация:**
- `Icon` — иконка корабля

**Код:**
- `emoji` — эмодзи корабля

**Влияние:** Минимальное, выбор представления

---

## Сводная таблица критичности

| Категория | Количество | Требует действий |
|-----------|-----------|------------------|
| **A (КРИТИЧНЫЕ)** | 6 | Архитектурные изменения, рефакторинг |
| **B (ВАЖНЫЕ)** | 5 | Рефакторинг, исправления |
| **C (НЕСУЩЕСТВЕННЫЕ)** | 2 | Опционально |

---

## Рекомендации по приоритетам

### Высокий приоритет (СДЕЛАТЬ В v0.5.0):
1. **A3** - Реализовать Pancreas как отдельный орган с Rules
2. **A6** - Обновить Muscle Rate Tiers согласно документации
3. **B3** - Исправить UI название "Muscle Boost" → "Pancreas Boost"
4. **B4** - Обновить Metformin DecayRate

### Средний приоритет (СДЕЛАТЬ В v0.6.0):
1. **A1** - Стандартизировать терминологию (LiverContainer vs liver)
2. **A2** - Переименовать Treatment → Substance
3. **A4** - Реализовать Threshold-based Effect Containers
4. **B1** - Стандартизировать Ship interface (DisplayedName, Icon)

### Низкий приоритет (СДЕЛАТЬ В v0.7.0+):
1. **A5** - Реализовать полную Degradation System с Tiers
2. **B5** - Реализовать Kidneys логику
3. **C1-C2** - Косметические улучшения

---

**Дата анализа:** 2026-02-04
**Версия кода:** v0.4.0
**Версия документации:** v0.6
